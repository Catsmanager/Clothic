# User Action Items

기준일: 2026-06-01

이 문서는 Codex가 코드로 처리하기 어렵거나, 사용자 계정/콘솔/심사 권한이 필요한 작업을 정리한 목록이다.

## 바로 할 일

- [ ] PR #34 확인 후 merge
  - PR: https://github.com/Catsmanager/Clothic/pull/34
  - 제목: `feat: 챌린지 화면 구현 (mock)`
  - 상태: CI 통과
  - 확인 포인트: 챌린지 화면 진입, 홈 헤더 award 아이콘 동작, mock UI 문구

- [ ] Kakao OAuth KOE205 해결
  - 카카오 개발자 콘솔에서 `account_email` 동의항목을 `선택 동의`로 활성화
  - 현재 코드에서는 Supabase GoTrue가 `account_email` scope를 기본 주입하므로 코드에서 제거하는 방식으로 해결하지 않는다.
  - 설정 후 카카오 로그인 인증창 진입과 로그인 완료 흐름을 다시 확인한다.

- [ ] Supabase `outfits.is_favorite` 컬럼 확인 또는 마이그레이션
  - 코드는 `is_favorite`이 없어도 깨지지 않도록 방어되어 있지만, 즐겨찾기 기능을 정상 사용하려면 DB 컬럼이 필요하다.
  - 예상 컬럼: `is_favorite boolean not null default false`
  - 적용 후 코디 상세/목록에서 즐겨찾기 토글 저장 여부를 확인한다.

## 에셋 제작

- [ ] 상의 PNG 10종 제작
- [ ] 하의 PNG 8종 제작
- [ ] 신발 PNG 6종 제작
- [ ] 가방 PNG 5종 제작
- [ ] 액세서리 PNG 4종 제작
- [ ] 제작한 PNG를 `assets/avatar/{category}/` 구조에 배치
- [ ] `constants/itemCatalog.ts`의 `imagePath`와 실제 파일명 일치 여부 확인

참고: 현재 아이템 메타데이터 33종은 등록되어 있지만, 실제 착장 PNG는 대부분 placeholder 상태다.

### 아바타/의상 에셋 상세 규격

기준 규격은 `assets/avatar/README.md`와 `docs/ASSET_PLAN.md`의 64×128px 레이어 방식이다. 모든 base/상의/하의/신발/가방/액세서리는 **같은 캔버스 크기**로 제작해야 하며, 개별 옷 모양에 맞춰 crop하면 안 된다.

#### 기본 캔버스

- 기준 캔버스: `64×128px`
- 2x export: `128×256px`, 파일명은 `@2x` 접미사 사용
- 형식: PNG
- 배경: 완전 투명
- 색상: 32-bit RGBA
- 정렬: 모든 레이어의 좌상단 기준점은 `(0, 0)`으로 동일
- 금지: 배경색 포함, 흰 배경 포함, 의상만 crop한 작은 PNG, 레이어마다 다른 캔버스 크기

주의: 현재 저장소의 `assets/avatar/base/base_female_01.png`는 실제 파일 기준 `256×256px`이다. 문서상 목표 규격인 `64×128px`과 다르므로, 의상 레이어 제작 전에 다음 중 하나를 결정해야 한다.

- 권장: base를 `64×128px`로 다시 export하고 모든 의상도 `64×128px`로 맞춘다.
- 대안: 기존 `256×256px` base를 유지한다면 모든 의상/액세서리도 `256×256px` 같은 캔버스로 맞춘다. 단, 이 경우 `assets/avatar/README.md`, `docs/ASSET_PLAN.md`의 규격도 함께 갱신해야 한다.

#### 64×128px 기준 좌표 가이드

아래 좌표는 제작자가 같은 위치에 옷을 맞추기 위한 권장 바운딩 박스다. 실제 픽셀 아트는 이 범위 안에서 형태에 맞게 조정하되, 캔버스 자체는 항상 `64×128px`로 유지한다.

| 요소 | 권장 영역 | 설명 |
| --- | --- | --- |
| 머리 | `x 22-42`, `y 8-34` | 얼굴/머리 기본 위치 |
| 목 | `x 28-36`, `y 34-40` | 상의 collar 기준 |
| 상체 | `x 17-47`, `y 38-72` | 티셔츠/블라우스/니트 중심 영역 |
| 팔 | `x 11-53`, `y 40-82` | 긴팔/반팔이 닿는 최대 영역 |
| 허리 | `x 20-44`, `y 66-76` | 상의와 하의가 만나는 기준선 |
| 다리 | `x 21-43`, `y 72-110` | 팬츠/스커트/레깅스 기준 |
| 발 | `x 16-49`, `y 106-122` | 신발 기준 |
| 바닥 여백 | `y 123-127` | 그림자가 없다면 비워둔다 |

#### 카테고리별 권장 크기

| 카테고리 | 캔버스 | 실제 그림 권장 영역 | 제작 메모 |
| --- | --- | --- | --- |
| base | `64×128px` | 전신 `x 14-50`, `y 6-122` | 몸 전체 기준. 모든 의상은 이 base 위에서 맞춘다. |
| top | `64×128px` | `x 13-51`, `y 36-76` | 민소매/티/블라우스/니트 모두 같은 기준점 사용. 긴팔은 `y 82`까지 가능. |
| bottom | `64×128px` | `x 17-47`, `y 66-108` | 쇼츠는 `y 88` 근처, 팬츠/레깅스는 `y 110` 근처까지. |
| shoes | `64×128px` | `x 15-50`, `y 104-122` | 양발 위치가 base 발과 정확히 겹쳐야 한다. |
| bag | `64×128px` | `x 6-58`, `y 44-94` | 숄더/크로스백 끈은 상체 위에 올라오므로 top보다 위 레이어에서 자연스럽게 보이게 제작. |
| accessory: hat | `64×128px` | `x 17-47`, `y 2-22` | 머리 위 여백 포함. |
| accessory: sunglasses | `64×128px` | `x 22-42`, `y 20-30` | 얼굴 중앙에 맞춘다. |
| accessory: scarf | `64×128px` | `x 16-48`, `y 33-52` | 목/어깨 위에 걸치는 형태. |
| accessory: none | 없음 | 없음 | `imagePath: ''`, PNG 제작하지 않는다. |

#### 2x 제작 시 좌표 변환

2x 파일은 모든 수치를 정확히 2배로 키운다.

- 기준 캔버스 `64×128px` → 2x 캔버스 `128×256px`
- top 권장 영역 `x 13-51`, `y 36-76` → `x 26-102`, `y 72-152`
- shoes 권장 영역 `x 15-50`, `y 104-122` → `x 30-100`, `y 208-244`

픽셀 아트 선명도를 위해 2x 파일은 벡터처럼 다시 그리기보다 64×128 원본을 nearest-neighbor 방식으로 2배 확대하는 것을 권장한다.

#### 레이어 순서

앱 렌더링 기준 레이어 순서는 아래와 같다.

1. `base`
2. `bottom`
3. `shoes`
4. `top`
5. `bag`
6. `accessory`

가방 끈이나 스카프처럼 다른 옷 위에 올라와야 하는 요소는 해당 레이어 순서를 고려해 제작한다. 예를 들어 크로스백 끈은 `bag` 레이어에 포함하고, top 위에 보이는 것을 전제로 그린다.

#### 선/스타일 권장

- 외곽선: 1px
- 그림자: 1px 단위의 작은 명암만 사용
- 안티앨리어싱: 픽셀 아트 선명도를 위해 최소화
- 색상 수: 아이템당 3-6색 권장
- 투명 여백: 남는 영역은 alpha 0으로 유지
- 스타일: 게임 캐릭터보다 라이프스타일 앱에 맞는 차분한 픽셀 아트

#### 파일명 체크리스트

- base: `assets/avatar/base/base_female_01.png`
- top: `assets/avatar/top/top_001_black.png` 형식
- bottom: `assets/avatar/bottom/bottom_001_denim.png` 형식
- shoes: `assets/avatar/shoes/shoes_001_white.png` 형식
- bag: `assets/avatar/bag/bag_001_beige.png` 형식
- accessory: `assets/avatar/accessory/accessory_001_hat.png` 형식

파일명은 `constants/itemCatalog.ts`의 `imagePath`와 정확히 일치해야 한다.

## 전체 검증

- [ ] `TEST_PLAN.md` 기준 수동 검증 실행
- [ ] 인증 플로우 확인
  - 이메일 회원가입
  - 이메일 로그인
  - 로그아웃
  - 온보딩 완료 상태 유지
  - 카카오 로그인
- [ ] 코디 플로우 확인
  - 코디 생성
  - 코디 저장
  - 코디 목록 조회
  - 코디 상세 조회
  - 즐겨찾기 토글
  - 삭제
- [ ] 탭 화면 확인
  - 홈
  - 캘린더
  - 월간 통계
  - 잠자는 옷장
  - 챌린지

## 빌드와 배포

- [ ] Expo 계정 로그인 및 EAS 권한 확인
- [ ] iOS Bundle ID `com.clothic.app` 사용 가능 여부 확인
- [ ] Android package name `com.clothic.app` 사용 가능 여부 확인
- [ ] iOS 빌드 실행
- [ ] Android 빌드 실행
- [ ] App Store Connect 앱 생성
- [ ] Google Play Console 앱 생성
- [ ] 스토어 심사용 스크린샷, 설명, 개인정보 처리방침 URL 준비

## 결정이 필요한 항목

- [x] 챌린지 화면을 mock 상태로 MVP에 포함할지 결정 → 해소: 챌린지는 mock이 아니라 lib/challenges.ts에서 outfits/items 실데이터로 동작. 그대로 MVP 포함 (2026-06-15 확인)
- [x] 잠자는 옷장 화면을 mock 상태로 유지할지, Supabase 데이터 기반으로 전환할지 결정 → 해소: 이슈 #80에서 outfits 실데이터 역산으로 전환 완료
- [ ] 아이템 PNG 제작 전까지 placeholder 색상 UI를 유지할지 결정
- [ ] 출시 전 카카오 로그인을 필수로 포함할지, 이메일 인증만으로 먼저 출시할지 결정
